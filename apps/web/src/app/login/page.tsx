export default function LoginPage() {
  return (
    <main style={{ padding: '2rem', maxWidth: 400, margin: '0 auto' }}>
      <h1 style={{ marginBottom: '1rem' }}>ログイン</h1>
      <p style={{ color: 'var(--muted)', marginBottom: '1.5rem' }}>
        Identity Platform 連携は実装中です。
      </p>
      <p>
        <a href="/">トップへ</a>
      </p>
    </main>
  );
}
